\section{Threat Model and Problem Formulation}
\label{sec:threat_model}

\subsection{Adversary Profiles}

\textbf{TURRET OS} evaluates adversary profiles across two corpora. On the established CERT-style insider-threat lineage corpus (D3 TSEC), the embedded adversary baseline consists of five synthetic insider scenarios generated over 500 users, 365 days, and 141,657 user-day activity records, with a 1.68\% positive rate \citep{wei2024ewatcher, xiao2024sentinel, gao2023dtgi}. On the 35-user, 90-day D5 Synthetic-Tenant Pilot (2,431 activity records), a held-out \texttt{OOD\_\allowbreak LAB\_\allowbreak EXFILTRATOR} profile is injected into the generator without appearing in the training split; this deliberately out-of-distribution scenario functions as a true hold-out evaluation. Across both corpora, four canonical attacker models anchor the adversarial evaluation track: Mimicry, Metadata-Strip, Identity-Proxy (\texttt{GHOST\_\allowbreak AUTHOR}), and OOD Credential-Phishing. The OOD Credential-Phishing adversary operates during business hours using stolen credentials with low feature novelty, identity-proxy reuse, and removable-media transfers, representing the regime in which classical novelty-driven insider detectors collapse. Table~\ref{tab:adversary_catalogue} catalogs the adversary profiles.

\begin{table*}[t!]
\caption{Adversary Profile Catalogue across Evaluation Corpora and Attacker Models.}
\label{tab:adversary_catalogue}
\centering
\footnotesize
\renewcommand{\arraystretch}{1.35}
\setlength{\tabcolsep}{6pt}
\begin{tabularx}{\textwidth}{l X X X X}
\toprule
\textbf{Profile ID} & \textbf{Population / Split} & \textbf{Profile Type} & \textbf{Operational Regime} & \textbf{Notation in Evaluation} \\
\midrule
D3-Trained & 5 synthetic insider scenarios, trained inside D3 & In-distribution latent adversary & 500 users $\times$ 365 days $\times$ 141,657 user-day records & Reference rows in \S6.2 \\
D3-Mimicry & Attacker vector \S3.2 & Evasion (feature mimicry) & Benign-neighbour cloning of feature distributions & Table~\ref{tab:adversarial_robustness} (baseline) \\
D3-Metadata-Strip & Attacker vector \S3.2 & Evasion (artefact redaction) & Author/revision-id signals stripped from OOXML/DWG/EML/git & Table~\ref{tab:adversarial_robustness} (baseline) \\
D3-Identity-Proxy & Attacker vector \S3.2 (\texttt{GHOST\_\allowbreak AUTHOR}) & Evasion (identity substitution) & Clean user's credential surface substituted & Table~\ref{tab:adversarial_robustness} (baseline) \\
D3-OOD Credential-Phishing & Attacker vector \S3.2 & OOD adversary & Business hours, low novelty, stolen creds, removable transfers & Table~\ref{tab:adversarial_robustness} ($+0.2209$ AUC rescue) \\
D5-OOD\_LAB\_EXFILTRATOR & Held-out injected profile & True hold-out OOD & Outside training split & \S6.2 (ROC-AUC 0.9928 anchor) \\
\bottomrule
\end{tabularx}
\end{table*}

\subsection{Attack Vectors and Evasion Strategies}

Four evasion strategies are exercised. Mimicry approximates benign-feature distributions to defeat single-layer feature detectors. Metadata-Strip Evasion removes authorship and revision-id signals from OOXML \citep{didriksen2014ooxml, fu2015docx}, PDF, DWG, EML, git, and image artefacts \citep{umair2024exif2vec, yasenenko2025evaluating}; this vector specifically targets L1 metadata-only detectors. Identity-Proxy Injection substitutes a clean user's credential surface for the attacker's actions, collapsing naive identity-conditioned rules. OOD Credential-Phishing combines stolen legitimate credentials, business-hours operation, no metadata-stripping, low feature novelty, identity-proxy reuse, and removable-media transfers, representing the regime in which novelty-, off-hours-, and metadata-driven insider detectors systematically collapse \citep{ren2022cskg4apt, kurniawan2022krystal}. The four vectors span the limiting cases any deployable insider-threat detector must survive end-to-end. Table~\ref{tab:attack_evasion_matrix} presents the evasion matrix.

\begin{table*}[t!]
\caption{Attack-Vector Evasion Matrix and Targeted Architectural Counter-Layers.}
\label{tab:attack_evasion_matrix}
\centering
\footnotesize
\renewcommand{\arraystretch}{1.25}
\setlength{\tabcolsep}{6pt}
\begin{tabularx}{\textwidth}{l X X X c}
\toprule
\textbf{Vector} & \textbf{Targeted Layer} & \textbf{Defeated Mechanism} & \textbf{Counter-Layer in TURRET OS} & \textbf{Multi-Layer AUC Drop} \\
\midrule
Mimicry & L3.5 (single-layer feature) & Feature novelty histograms & L1 authorship + L2 graph topology & $\le 5\%$ \\
Metadata-Strip & L1 (metadata-only) & OOXML/DWG/EML/git redaction & L2 \texttt{SAME\_\allowbreak FINGERPRINT\_\allowbreak AS} graph edges & $\le 5\%$ \\
Identity-Proxy & Identity-conditioned rules & Clean user credentials substituted & L3 R8 + L1 metadata cross-anchor & $\le 5\%$ \\
OOD Credential Phishing & All single-layer detectors & Business hours, stolen creds, low novelty & Full multi-layer fusion & 4.12\% vs 26.25\% (single) \\
\bottomrule
\end{tabularx}
\end{table*}

\subsection{Forensic Readiness Requirements}

Detection must be coupled to a forensic-evidence chain admissible under ISO/IEC 27043 \citep{shoderu2025dfrsaas, shoderu2026dfrconference}. The architectural target is 100\% attribute coverage across the eight mandated attributes: Identification (unique record and alert UIDs), Collection (documented harvest-pipeline provenance), Acquisition (SHA-256 + BLAKE3 hash verification), Preservation, Analysis (SHAP + GNNExplainer provenance subgraphs), Presentation (analyst-UI workbench export format), Chain of Custody (timestamps and operator-signature tracking), and Integrity Verification (Ed25519 signature over the Merkle root) \citep{shoderu2025dfrsaas}. This requirement elevates the threat model from "raise an alert" to "raise an alert admissible as evidence under a forensic readiness audit". Table~\ref{tab:iso27043_mapping} maps the implementation to ISO/IEC 27043 attributes.

\begin{table*}[t!]
\caption{ISO/IEC 27043 Attribute-Coverage Mapping for Layer-4 Tamper-Evident Evidence Packaging.}
\label{tab:iso27043_mapping}
\centering
\footnotesize
\renewcommand{\arraystretch}{1.25}
\setlength{\tabcolsep}{6pt}
\begin{tabularx}{\textwidth}{l X X c}
\toprule
\textbf{ISO/IEC 27043 Attribute} & \textbf{Mandatory Role} & \textbf{TURRET OS Implementation} & \textbf{Status} \\
\midrule
Identification & Unique record + alert UIDs & L4 emits \texttt{Alert-UID} + \texttt{Provenance-Bundle} UID pair per alert & \checkmark \\
Collection & Documented harvest pipeline & L1 cross-format harvesters verified by unit tests & \checkmark \\
Acquisition & Hash-verified artefact chain & SHA-256 + BLAKE3 dual hash on every raw artefact & \checkmark \\
Preservation & WORM-archived storage & Layered chain-of-custody storage, 4.8 GB WORM for D5 & \checkmark \\
Analysis & Provenance subgraphs & SHAP feature importance + GNNExplainer subgraph export & \checkmark \\
Presentation & Analyst-UI workbench & W3C-PROV document + audit export through L4 & \checkmark \\
Chain of Custody & Operator signature + timestamps & Operator-identity logger with Ed25519 over Merkle root & \checkmark \\
Integrity Verification & Tamper-evident sealing & Ed25519 digital signature on every Merkle root & \checkmark \\
\midrule
\textbf{Coverage} & -- & -- & \textbf{100\%} \\
\bottomrule
\end{tabularx}
\end{table*}

\subsection{Detection and Robustness Evaluation Criteria}

Detection is reported on three primary metrics: ROC-AUC, F1 @ 0.5\% FPR (the precision-critical operating point for low-false-positive deployability), and Matthews Correlation Coefficient (MCC). Calibration is performed via \texttt{CalibratedClassifierCV} (isotonic, cv=3); operating thresholds are swept via Youden's J statistic. Target on D3 TSEC: \textbf{ROC-AUC 0.9997, F1@0.5\%FPR 0.8774, MCC 0.9770}, with the operating threshold pinned at the 0.5\% FPR constraint to match the deployment. Target on the D5 \texttt{OOD\_\allowbreak LAB\_\allowbreak EXFILTRATOR} hold-out: \textbf{ROC-AUC 0.9928, F1 0.8936, MCC 0.9539}. Adversarial robustness is bound by \textbf{AUC-drop $\le$ 5\%} against each of the four attacker vectors in \S3.2. Cross-layer attribution is bound by a runtime safety invariant: the top-5 SHAP feature importances on the chronological train/test split must belong to a fixed operational whitelist (\texttt{access\_\allowbreak novelty\_\allowbreak score}, \texttt{metadata\_\allowbreak stripped}, \texttt{off\_\allowbreak hours\_\allowbreak multiplier}, \texttt{identity\_\allowbreak proxy}, \texttt{copy\_\allowbreak to\_\allowbreak removable}, \texttt{n\_\allowbreak file\_\allowbreak accesses}, \texttt{outbound\_\allowbreak email}, \texttt{access\_\allowbreak hour}), guaranteeing zero label leakage at deployment.
